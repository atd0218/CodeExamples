import logo from './logo.svg';
import './App.css';

export function Header() {
    let title = "This is some existing text";
    return (
      //use jsx to return the title in a header tag. 
      <h1>{title}</h1>
    );
  }